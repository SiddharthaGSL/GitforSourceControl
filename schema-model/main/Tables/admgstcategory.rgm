table "main"."admgstcategory" {
  primaryKey = {
    columns = [
      "code"
    ]
    name = "admgstcategory_pk"
    objectType = primaryKey
  }
  columns = (
    column "code" {
      type = bigint
      null = NOT NULL
    }
    column "name" {
      type = character varying(100)
      null = NOT NULL
    }
    column "display_seq" {
      type = bigint
      null = NOT NULL
    }
  )
}

