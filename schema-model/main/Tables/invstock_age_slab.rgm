table "main"."invstock_age_slab" {
  primaryKey = {
    columns = [
      "code"
    ]
    name = "invstock_age_slab_pkey"
    objectType = primaryKey
  }
  columns = (
    column "code" {
      type = bigint
      null = NOT NULL
    }
    column "slab_name" {
      type = character varying(100)
    }
    column "slab_day_from" {
      type = bigint
    }
    column "slab_day_to" {
      type = bigint
    }
  )
}

