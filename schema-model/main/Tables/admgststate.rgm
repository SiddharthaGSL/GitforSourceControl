table "main"."admgststate" {
  primaryKey = {
    columns = [
      "code"
    ]
    name = "admgststate_pk"
    objectType = primaryKey
  }
  columns = (
    column "code" {
      type = character(2)
      null = NOT NULL
    }
    column "name" {
      type = character varying(50)
      null = NOT NULL
    }
    column "abb" {
      type = character(2)
      null = NOT NULL
    }
  )
}

