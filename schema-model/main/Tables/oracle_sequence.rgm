table "main"."oracle_sequence" {
  columns = (
    column "sequence_name" {
      type = character varying(128)
      null = NOT NULL
    }
    column "last_number" {
      type = integer
      null = NOT NULL
    }
  )
}

