table "main"."promo_benefits" {
  primaryKey = {
    columns = [
      "code"
    ]
    name = "promo_benefits_code_pk"
    objectType = primaryKey
  }
  columns = (
    column "code" {
      type = bigint
      null = NOT NULL
    }
    column "name" {
      type = character varying(50)
      null = NOT NULL
    }
  )
}

