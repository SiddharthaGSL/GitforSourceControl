view "main"."mlv_year" {
  text = """-
SELECT y.ycode AS year_code,
    y.yname AS year_name
   FROM main.admyear y;
"""
  columns = (
    column "year_code" {
      type = "bigint"
    }
    column "year_name" {
      type = "character varying(100)"
    }
  )
}

