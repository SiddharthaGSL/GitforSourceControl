view "main"."v_psite_cat1" {
  text = """-
SELECT invcat1.ccode AS ccode1,
    invcat1.cname AS cat1
   FROM main.invcat1
  ORDER BY invcat1.cname;
"""
  columns = (
    column "ccode1" {
      type = "bigint"
    }
    column "cat1" {
      type = "character varying(100)"
    }
  )
}

