view "main"."mlv_stockpoint" {
  text = """-
SELECT l.loccode,
    l.locname
   FROM main.invloc l;
"""
  columns = (
    column "loccode" {
      type = "bigint"
    }
    column "locname" {
      type = "character varying(100)"
    }
  )
}

