view "main"."v_production_group" {
  text = """-
SELECT a.grpname AS group_name,
    a.grpcode AS group_code
   FROM main.fingrp a;
"""
  columns = (
    column "group_name" {
      type = "character varying(100)"
    }
    column "group_code" {
      type = "bigint"
    }
  )
}

